import styled from 'styled-components';

export const Container = styled.span`
  font-size: 1.4rem;
  font-weight: 800;
  color: var(--white);

  @media(max-width: 1024px){
    font-size: 1.2rem;
  }
`;
